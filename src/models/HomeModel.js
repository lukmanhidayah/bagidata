class HomeModel {
  constructor(id, title, description, imageUrl, navigation, color) {
    this.id = id;
    this.title = title;
    this.description = description;
    this.imageUrl = imageUrl;
    this.navigation = navigation;
    this.color = color;
  }
}

export default HomeModel;
